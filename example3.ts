const x = process.cwd();
const x1 = getComputedStyle;
const x2 = globalThis.AbortController;
const x3 = globalThis.getComputedStyle;
const x4 = AbortSignal.timeout;
globalThis.process.abort;
import.meta.env.VITE_APP_TITLE;
import.meta.url;
console.log('asd');
const x6 = { ...Object };

class Album {
  constructor() {}

  download() {
    // Default behavior
  }
}

class SharedAlbum extends Album {
  download() {
    // Override to get info from many sources
  }
}
