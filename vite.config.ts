import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';

const x = AggregateError?.x;
const x1 = { ...[] };

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [vue()],
});
